import searchBar from "../searchBar.module.scss";

export const DateRangePopup = () => {
  return (
    <div className={searchBar["pop-up"]} style={{ left: 0 }}>
      <p>Date Range</p>
    </div>
  );
};
